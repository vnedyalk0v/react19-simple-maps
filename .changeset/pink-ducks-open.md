---
'@vnedyalk0v/react19-simple-maps': major
---

BREAKING: Package is now ESM-only. CommonJS (`require`) and UMD builds are removed.
Added `./utils` subpath export for direct utility imports.
Publish target updated to GitHub Packages (npmjs.org becomes optional).
